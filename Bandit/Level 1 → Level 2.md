# Level 1 → Level 2

```bash
bandit1@bandit:~$ cat ./-
```

---

The file is named as “**`-`**” if I used any reading tools it will not read it, so I used “**`./`**” **explicitly tells the system to look in the current directory**