# Level 17 → Level 18

```bash
bandit17@bandit:~$ diff passwords.new passwords.old
```

---

**`diff`** — (**Difference**) compares two text files or directories line-by-line and outputs the exact changes required to convert one into the other